apiVersion: v1
kind: Service
metadata:
  name: {{ include "ganje-admin-portal.fullname" . }}
  labels:
    {{- include "ganje-admin-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ganje-admin-portal.selectorLabels" . | nindent 4 }}
